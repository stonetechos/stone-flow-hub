import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.stonetech.erp',
  appName: 'Stone Tech OS',
  webDir: '.output/public'
};

export default config;
