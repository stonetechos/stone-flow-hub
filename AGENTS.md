> [!IMPORTANT]
> Deploys to production run via `.github/workflows/deploy.yml` (GitHub
> Actions → Cloudflare Workers, triggered after CI passes on `main`) — not
> Lovable's Publish button. See `docs/DEPLOYMENT.md`.
