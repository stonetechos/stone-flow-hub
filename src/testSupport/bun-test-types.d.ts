/**
 * Ambient module declaration for bun:test.
 *
 * Import only Bun's test typings so they are available to the test
 * TypeScript program without pulling in Bun's full runtime globals.
 */

import "bun-types/test";

export {};
